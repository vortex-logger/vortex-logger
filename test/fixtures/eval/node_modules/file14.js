const bingo = require("../../../../");

module.exports = function() {
    const logger = bingo(
        bingo.transport({
            target: 'bingo-logger/file'
        })
    )

    logger.info('done!')
}
