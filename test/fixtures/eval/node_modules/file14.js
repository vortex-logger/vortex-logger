const zenlog = require("../../../../zenlog");

module.exports = function() {
    const logger = zenlog(
        zenlog.transport({
            target: 'zenlog/file'
        })
    )

    logger.info('done!')
}
